{
  "name": "bloom-v2",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev -w @bloom/api\" \"npm run dev -w @bloom/web\"",
    "build": "npm run build -w @bloom/contracts && npm run build -w @bloom/api && npm run build -w @bloom/web",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
